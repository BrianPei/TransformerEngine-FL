name: enflame_tests

on:
  workflow_dispatch:

jobs:
  select_enflame_branch:
    runs-on: ubuntu-latest
    steps:
      - name: Select the Enflame development branch
        run: |
          echo "This entry registers Enflame tests in the Actions page."
          echo "Run it again and select Enflame-dev from the branch list."
          exit 1
