name: musa_tests

on:
  workflow_dispatch:

jobs:
  select_musa_branch:
    runs-on: ubuntu-latest
    steps:
      - name: Select the MUSA development branch
        run: |
          echo "This entry registers MUSA tests in the Actions page."
          echo "Run it again and select musa-dev from the branch list."
          exit 1
